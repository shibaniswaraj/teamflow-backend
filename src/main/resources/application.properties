spring.application.name=teamflow
# ================================
# DATABASE CONFIGURATION (AWS RDS)
# ================================

spring.datasource.url=jdbc:mysql://teamflow-db.cn2a82a60ri1.ap-south-1.rds.amazonaws.com:3306/teamflow?useSSL=false&allowPublicKeyRetrievaltrue
spring.datasource.username=admin
spring.datasource.password=Teamflow17!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =================================================
# JPA / HIBERNATE - CREATE/UPDATE DATABASE SCHEMA
# =================================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===========================
# SERVER CONFIG
# ===========================
server.port=8080

